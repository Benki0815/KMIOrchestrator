# Secrets

Credentials live in the workspace root as `.env` (gitignored).

| Variable | Source | Purpose |
|----------|--------|---------|
| `SOFASCORE_RAPIDAPI_KEY` | BallistiXG (RapidAPI apisummit) | Player images, season KPIs, form data |
| `OPENROUTER_API_KEY` | User-provided (new key pending) | LLM adjustments & explanations |

Template: `/.env.example`
