# Journal 2026-08-24 – KMI Orchestrator

## Session 14:51 – Schalke-Kader fehlte in der SDB

**Thema:** SDB zeigte unter S04 nur Adamu + Wöber; Aufsteiger-Kader waren ingest-seitig `active=false`.

**Kernaussagen:**
- Scout-DB hatte 35 Schalke-Spieler, KMI-API nur 2 aktiven (Adamu/Wöber, `points_source=BL`).
- `push_to_kmi.py` nahm nur `points_source == "BL"` in den Pool. 2L/None (Aufsteiger-Vorsaison) wurde `active=false`, API filtert Inactive raus.
- Gleicher Effekt bei Elversberg (29) und Paderborn (33).
- Die meisten Schalke-Spieler haben noch keine gematchten 2L-Vorjahrespunkte (`points_source=None`) – sichtbar, aber PKT oft 0.

**Entscheidungen:**
- Pool-Regel: aktueller BL-Verein 2026/27 zählt, nicht nur BL-Vorsaisonspunkte.
- KMI-Ingest aktiviert Spieler mit `CURRENT_BUNDESLIGA_CODES` defensiv mit.

**Konkrete Artefakte / Befehle / Pfade:**
- `C:\Dev\privateTemp\kicker-scout\scripts\push_to_kmi.py` – `current_pool_teams`
- `backend/app/main.py` – `CURRENT_BUNDESLIGA_CODES`, `v.0824.003`
- `docs/kader-ingest.md`
- `python scripts/push_to_kmi.py` → 599 imported
- `ssh dockerhost274-kmi` rebuild backend+frontend
- Live: Schalke 35, Elversberg 29, Paderborn 33; UI `35 TREFFER`
- Frontend DnD-Fix parallel auf `v.0824.006` (kein `<button draggable>`)

**Offen / Naechste Schritte:**
- 2L-Punkte-Match für Schalke (Karaman & Co.) in kicker-scout nachziehen
- DnD am NAS manuell gegenprüfen (SDB → Feld/Bank, gleiche Position)
- Git: fast das ganze Projekt untracked; Commit/Push nur nach Freigabe

## Session 15:04 – Aufsteiger 2L-Punkte (Elversberg/Paderborn/Schalke)

**Thema:** Alle drei Aufsteiger-Kader sichtbar machen und Vorsaisonspunkte nachziehen.

**Kernaussagen:**
- Kaderzahlen waren nach dem Pool-Fix schon live: SVE 29, SCP 33, S04 35.
- `se-k00022025.csv` enthält im Aug 2026 den neuen 2L-Kader (Kiel/Bochum), Aufsteiger fast weg.
- Archiv `se-k00022024.csv` hat die Aufsteiger-Punkte (Karaman 187, Kristof 233, Noll 206). Match per Kicker-ID.

**Konkrete Artefakte / Befehle / Pfade:**
- `kicker-scout/scripts/fetch_kicker_data.py` – `LIGA2_ARCHIVE_URL`, `_liga2_points_row`
- `kicker-scout/app/seed.py` – Punkte auch bei bestehendem 0 überschreiben
- `python scripts/fetch_kicker_data.py` → 40 Aufsteiger mit 2L-Punkten
- `python app/seed.py` + `python scripts/push_to_kmi.py` → 600 imported
- UI: Elversberg 29 TREFFER, Paderborn 33 TREFFER; pkt>0: SVE 17, SCP 16, S04 11

**Offen / Naechste Schritte:**
- Rest ohne 2L-Punkte = Sommer-Neuzugänge (Dzeko, Gosens, …), kein CSV-Match
- DnD am NAS manuell gegenprüfen

## Session 15:12 – Eric Martel Verletzung

**Thema:** Knieverletzung bei Eric Martel eintragen, falls leer.

**Kernaussagen:**
- Spieler `pl-k00119414` hatte `injury=null`. Im Team-Datensatz stand fälschlich "Erik Martel" – Name-Match schlug fehl.
- SofaScore 24.08.2026: Knie, seit 01.08.2026, Rückkehr September 2026.

**Konkrete Artefakte / Befehle / Pfade:**
- `kicker-scout/extraction/teams_extracted.json` – Quelle SofaScore
- `python scripts/push_teams_to_kmi.py`
- `backend/app/main.py` – Nachname-Fallback, `v.0824.004`
- Live: `type=Knie`, `expectedReturn=Rückkehr September 2026`

## Session 15:40 – SofaScore-MISSING Verletzungen

**Thema:** Screenshots der MISSING-Listen gegen Live-Kader prüfen; leer eintragen, sonst updaten.

**Kernaussagen:**
- 45 Spieler aus 18 BL-Vereinen gematcht. 42 neu, 3 Updates (Seimen, Castrop, Martel).
- Y. Konoplya = Yukhym Konoplia (Gladbach); S. K. Mvumpa = Silas (Mainz).
- HSV-Name in der Quelle war `Røssing` (ø), Live-Kader `Rössing` (ö) – Match erst nach Namensangleichung.
- Bestehende andere Verletzungen (Nebel, Asllani, Muheim, Uno, Koch, …) bleiben per Union erhalten.

**Konkrete Artefakte / Befehle / Pfade:**
- `C:\Dev\privateTemp\kicker-scout\extraction\teams_extracted.json` – SofaScore `Starting lineups (Expected) – MISSING`, `published=2026-08-24`
- `python C:\Dev\privateTemp\kicker-scout\scripts\push_teams_to_kmi.py` → 41 entries, 18 teams
- Live-API: 45/45 Screenshot-Spieler mit `type`+`expectedReturn`; Martel jetzt `Knieverletzung` / `bis 10.9.2026`

**Offen / Naechste Schritte:**
- Im UI (SDB-Badge) einmal Mainz/Silas und HSV/Rössing gegenprüfen

## Session 15:45 – Pitch-Chips rechts abgeschnitten

**Thema:** Entfernen/Tausch-Buttons am rechten Feldspieler waren hinter der Bank verdeckt.

**Kernaussagen:**
- Pitch war `max-w-[440px]` + `overflow-hidden`; Buttons lagen `-right-1.5` außerhalb und wurden abgeschnitten.
- Zusätzlich hat `pl-10` die Reihe nach rechts geschoben.

**Konkrete Artefakte / Befehle / Pfade:**
- Grid `220/520/300` statt `220/440/360` in `frontend/src/app/page.tsx`
- `PitchView.tsx` – Trikots 64px, Buttons im Chip-Box, kein `pl-10`
- Frontend `v.0824.007`, Build auf dockerhost274-kmi, `kmi-frontend` neu

**Offen / Naechste Schritte:**
- Rechts außen hover: X + Tausch sichtbar, auch bei 5er-Reihe (z. B. 3-5-2)

## Session 16:00 – Tauschen ohne Wirkung

**Thema:** Tausch-Buttons taten nichts Sichtbares.

**Kernaussagen:**
- `commitVariant` schrieb nur `swapInByOut`. Echter Kader-Replace lag in `applySwapPlan` auf **Präsentieren**, das in der Navi ausgeblendet ist.
- Grauer Button „Alternative wählen“ war `disabled` – Klick tot.
- Spieler-Card (z-120) lag über der Picker-Leiste „In Variante übernehmen“.

**Konkrete Artefakte / Befehle / Pfade:**
- `frontend/src/lib/store.ts` – `applySwapMapToTab`, `commitVariant` übernimmt Kader + wechselt zu Entwerfen
- `SwapPlanner.tsx` / `PlayerForecastModal.tsx`
- `docs/tauschen.md`
- Frontend `v.0824.008`, NAS-Rebuild

**Offen / Naechste Schritte:**
- Ein Tausch end-to-end im UI gegenprüfen (Wählen → Card → übernehmen → Variante übernehmen)

## Session 16:18 – Elversberg PlAIer-Scores

**Thema:** 12 Elversberg-Spieler mit PlAIer-Scores (Format 3.867 → 3867) in Scout-DB + Live-KMI.

**Kernaussagen:**
- Alle 12 Namen waren bereits im Pool (`plaier_score` vorher `NULL`).
- Deutsche Punkt-Schreibweise wie im MAIK-Schema: `3.867` = vierstellige Kennzahl `3867`.
- Niveau-Labels nicht gespeichert (kein `plaier_label`-Feld; `agent_label` nicht überschrieben).
- Keine neuen Mentions, damit Agent-Scores unangetastet bleiben. Scout-Seed nutzt `COALESCE` – Werte überleben Reseed.

**Konkrete Artefakte / Befehle / Pfade:**
- Scout-Backup `C:\Dev\privateTemp\kicker-scout\data\backups\kicker_scout_20260824_161604_pre_plaier.db`
- KMI-Backup `/app/data/backups/kmi_portal_20260824_141820.db`
- `python scripts/push_to_kmi.py` → 600 imported
- Live: 12/12 OK, `with_plaier` 57 → 69

**Offen / Naechste Schritte:**
- Niveau-Texte optional als Mention/`rating_label` nachziehen, falls eine MAIK-Folge als Quelle bekannt ist

## Session 19:45 – Persistentes Backend-Log

**Thema:** Log-View im Portal; alle Backend-Events in SQLite, Historie aus Mentions/Quellen nachgezogen.

**Kernaussagen:**
- Tabelle `activity_log` + `GET /api/logs`; Nav-Punkt **Log**.
- Pro Video/Podcast: geladen → transkribiert → Daten eingetragen (Spielerbesprechungen, Scores, Teams).
- Webquellen (z. B. SofaScore) nur als Ingest, nicht als Transkript.
- Live: 109 Einträge, neueste zuerst, Status OK + Kategorie.

**Konkrete Artefakte / Befehle / Pfade:**
- `backend/app/activity_log.py`, `frontend/src/components/LogPanel.tsx`
- `docs/activity-log.md`
- Version `v.0824.009`
- `ssh dockerhost274-kmi` rebuild frontend+backend
- http://192.168.178.251:3050/ Tab Log

**Offen / Naechste Schritte:**
- Git: weiterhin viel Untracked; Commit nur nach Freigabe

## Session 20:20 – Tausch auch auf der Bank

**Thema:** Ersatzspieler auf der Bank können wie Feldspieler zum Tausch markiert werden.

**Kernaussagen:**
- Store konnte Bank-Slots schon ersetzen; UI fehlte auf den Bank-Zeilen.
- Bank-Zeile: Tausch-Icon (Hover), pinker Ring wenn markiert.
- Tauschen-Tab: Chip-Badge **Bank**, Variante „statt X (Bank)“.
- Live geprüft: Karaman (Bank) markiert → Tauschen · 1 → Badge + Variantenzeile; Markierung danach geleert.

**Konkrete Artefakte / Befehle / Pfade:**
- `frontend/src/components/BankPanel.tsx`, `SwapPlanner.tsx`
- `docs/tauschen.md`
- Version `v.0824.010`
- `ssh dockerhost274-kmi` → `docker compose up -d --build frontend`
- http://192.168.178.251:3050/

**Offen / Naechste Schritte:**
- Git: weiterhin viel Untracked; Commit nur nach Freigabe

## Session 21:05 – Alternativliste mit allen KPIs

**Thema:** Tausch-Alternativliste zeigt Fav, Sterne, xPunkte und die restlichen Pool-Spalten.

**Kernaussagen:**
- Liste nutzt dieselben `buildListColumns` wie Spielerpool/Players Table.
- Extra: Liga, Fav (toggle), Sterne (editierbar), Fav-/Sterne-Filter.
- Sortierung inkl. xPunkte (Default), Fav, Sterne.
- Live: Diaz raus, Liste mit Fav/Sterne/xPunkte/PlAIer/SofaScore; Markierung geleert.

**Konkrete Artefakte / Befehle / Pfade:**
- `frontend/src/components/AlternativePickerModal.tsx`
- `docs/tauschen.md`
- Version `v.0824.011`
- `ssh dockerhost274-kmi` → `docker compose up -d --build frontend`
- http://192.168.178.251:3050/

**Offen / Naechste Schritte:**
- Git: weiterhin viel Untracked; Commit nur nach Freigabe

## Session 22:30 – Ligainsider-Kanal + Transcribe-Queue

**Thema:** YouTube-Kanal Ligainsider ins Scout-System, Backfill ab Union/Lustrinelli, Daily 09:00 als Queue.

**Kernaussagen:**
- Startvideo 21.05.2026: „So spielt UNION BERLIN unter dem neuen Trainer Mauro Lustrinelli!“ (`rRxOMU5A0V0`).
- 36 Langvideos heruntergeladen; Shorts im Fenster keine (neuester Clip 15.05.).
- Quelle `Ligainsider` Gewicht 1.0 wie MU; Dashboard-Pill amber.
- yt-dlp-`match_filter` hat MP3s nicht ins Manifest geschrieben — 36 Dateien nachgetragen.
- Transcribe speichert nach jeder fertigen Datei. PC-Shutdown: nur das laufende Video verloren, Rest `downloaded`. Daily 09:00 (`KickerScoutDailyUpdate`, unbegrenzt) macht nach dem letzten vollständigen Video weiter; Extraktion max. 24 Transkripte/Tag.
- Stand ~22:30: 3/36 transkribiert (Frankfurt, HSV, Werder), BVB lief; Whisper-Prozess stirbt beim Herunterfahren.

**Entscheidungen:**
- Daily-Job nach Transferfenster weiter (nur Kicker-CSV stoppt 02.09.).
- Whisper `language: null` (Auto-Detect, Ligainsider mixt DE/EN).

**Konkrete Artefakte / Befehle / Pfade:**
- `c:\Dev\privateTemp\kicker-scout\config.yaml` – Ligainsider + Shorts, since 2026-05-21
- `app/scoring.py` `SOURCE_WEIGHTS`, `scripts/fetch_youtube.py` ingest_listing_mp3s
- Windows-Task `KickerScoutDailyUpdate` 09:00, kein EndBoundary
- `docs/scout-quellen.md`, kicker-scout `README.md` Queue/Resume
- KMI Frontend `v.0824.012`, NAS-Rebuild frontend
- Transcribe-Log: `kicker-scout/data/daily_logs/ligainsider_transcribe.log`

**Offen / Naechste Schritte:**
- Restliche ~33 Ligainsider-MP3s transkribieren (Daily 09:00 oder laufenden Whisper anlassen)
- Extract + KMI-Push der neuen Mentions/Teams


