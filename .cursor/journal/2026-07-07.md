# Journal 2026-07-07 – KMI Orchestrator

## Session 19:45 – Basement-Setup

**Thema:** Neues Projekt-Workspace nach Basement-Template initial eingerichtet

**Kernaussagen:**
- Workspace-Struktur, Cursor Rules und Basis-Doku angelegt
- Git initialisiert, initialer Commit gepusht auf leeres GitHub-Repo
- DockerHost274 per SSH erreichbar; projektspezifischer Service-Alias noch offen

**Entscheidungen:**
- NAS-Ziel: DockerHost274 (nicht Benki-NAS3)
- `.github/hooks/` und `.github/pushme/` bleiben lokal (via `.gitignore`)

**Konkrete Artefakte / Befehle / Pfade:**
- `C:\Dev\KMI Orchestrator` – Workspace
- `.cursor/rules/project-baseline.mdc` – Agent-Baseline
- `.cursor/rules/daily-work-log.mdc` – Tagesjournal-Regel
- `docs/setup.md` – Projekt-Setup
- `docs/cicd.md` – CI/CD-Grundlage (Platzhalter für Deploy-Pfad)
- https://github.com/Benki0815/KMIOrchestrator.git – Remote
- `ssh dockerhost274 "echo SSH_OK"` – Verbindung OK

**Commits:** `ba72e27` – Initial project setup from Basement template

**Offen / Naechste Schritte:**
- Projektspezifischen SSH-Alias (z. B. `dockerhost274-kmi`) und Zielpfad auf NAS festlegen
- Container-Namen und Deploy-Ablauf in `docs/cicd.md` ergänzen
- Eigentliche Anwendungsarchitektur / Code-Struktur definieren
