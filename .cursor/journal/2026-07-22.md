# Journal 2026-07-22 – KMI Orchestrator

## Session 19:40 – MVP-Umsetzung Midnight Scout

**Thema:** Stitch-Mockup Runde 2 übernommen und Draft-Room MVP implementiert

**Kernaussagen:**
- Gewähltes Template: Midnight Scout (Variante A + Unterseiten im Zip)
- Frontend Next.js 14 lauffähig: Tabs, Sterne, Pitch, Spieler-DB, Auto-Save (localStorage)
- Backend FastAPI: Health, Players-Seed, Squad Persistenz
- Heuristische „Generate Suggestions“ respektiert Budget 42,5M und 3-7-7-5
- `npm run build` und Backend-Import erfolgreich

**Entscheidungen:**
- Branding aus Stitch: MIDNIGHT_SCOUT
- Version `v.0722.001`
- Demo-Daten statt Live-Kicker/SofaScore in Phase 1

**Konkrete Artefakte / Befehle / Pfade:**
- `frontend/` – Next.js App
- `backend/app/main.py` – API
- `docker-compose.yml` – Compose-Skelett
- `docs/architektur-mvp.md`
- `assets/Template Mockup/stitch_kicker_squad_architect.zip`
- Checks: `npm run build` ✅ · `from app.main import app` ✅

**Offen / Naechste Schritte:**
- CSV-Import, Solver, SofaScore, OpenRouter-Key
- NAS-Pfad festlegen und deployen
- Unterseiten Scouting/Transfermarkt ausbauen

## Session 20:05 – DockerHost274 Deploy + Homepage

**Thema:** KMI Orchestrator auf DockerHost274 gehostet, Homepage-Kachel mit LAN/Tailscale

**Kernaussagen:**
- Service-User `kmi-svc` (docker-Gruppe) + Sudoers fuer docker/docker-compose
- Stack laeuft: `kmi-frontend` :3050, `kmi-backend` :8050
- Mounts: `config` ro, `data` rw unter `/data/docker/kmi-orchestrator/`
- Homepage `id: kmi-orchestrator` mit Auto-Switch 192.168.178.251 / 100.84.97.16

**Entscheidungen:**
- Deploy-SSH-Alias `dockerhost274-kmi`
- Kein lokaler Dauerbetrieb; Ziel ist DockerHost274

**Konkrete Artefakte / Befehle / Pfade:**
- `ssh dockerhost274-kmi` → `/data/docker/kmi-orchestrator/`
- `/etc/sudoers.d/kmi-svc`
- LAN: http://192.168.178.251:3050/ · TS: http://100.84.97.16:3050/
- Homepage: `config/services.yaml` + `config/custom.js` deployt, `docker compose restart`
- `docs/cicd.md` finalisiert

**Offen / Naechste Schritte:**
- CSV-Import, Solver, SofaScore, OpenRouter-Key
- Unterseiten Scouting/Transfermarkt

## Session 20:12 – Black-Screen Fix

**Thema:** Frontend Crash nach erstem Paint behoben

**Kernaussagen:**
- Ursache: `getSquadMetrics()` im Zustand-Selector → neues Objekt pro Render → Endlosschleife
- Fix: `useSquadMetrics` / `useActiveTab` mit `useShallow`; stabile Default-Tab-IDs
- Redeploy `v.0722.002` auf DockerHost274

**Konkrete Artefakte / Befehle / Pfade:**
- `frontend/src/lib/store.ts`
- `frontend/src/components/{StatusBar,PitchView,PlayerDatabase}.tsx`
- Verify: health `v.0722.002`, frontend HTTP 200

## Session 20:20 – React 418/185 Fix

**Thema:** Client-Crash (Hydration + Max update depth) endgueltig adressiert

**Kernaussagen:**
- `useShallow` reichte nicht: nested `positions`-Objekt → weiterhin Endlos-Update (#185)
- Fix: Metrics via `useMemo`; Persist mit `skipHydration` + Rehydrate nach Mount
- Persist-Key auf `kmi-orchestrator-squads-v2`; Version `v.0722.003` live

**Konkrete Artefakte / Befehle / Pfade:**
- `frontend/src/lib/store.ts`, `StoreProvider.tsx`, `layout.tsx`
- Chunk-Hashes neu (`23-ac75b6…`) — alter Console-Stand war noch `23-8f358a…`

## Session 08:20 (Folgetag) – Layout Re-Design (Mockup <> Realitaet)

**Thema:** Naeher an Mockup-Anforderung ran (Breite, Spieler-Chips, Branding, Buttons)

**Kernaussagen:**
- Branding "Midnight Scout" verworfen, stattdessen "KMI DRAFT" + Shield-Logo-Aesthetik eingebaut
- max-width Container entfernt → Volle Bildschirmbreite genutzt (`px-8`)
- PitchView-Buttons ("Optimize", "Budget", "Suggestions", "Reshuffle") homogen im 4-Grid ausgerichtet
- Spielerchips auf dem Feld deutlich vergroessert, Marktwert in gelb (Amber) integriert
- Bank in saubere Gruppen (TOR, ABW, MIT, STU) getrennt

**Konkrete Artefakte / Befehle / Pfade:**
- `TopNav.tsx`, `page.tsx`, `StatusBar.tsx`, `Footer.tsx`
- `PitchView.tsx` komplett ueberarbeitet
- Version `v.0723.001` live deployt
