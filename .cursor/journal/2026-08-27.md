# Journal 2026-08-27 – KMI Orchestrator

## Session 14:32 – Transfer-Kader CSV-Overlay

**Thema:** Transfers vom 25.08. fehlten in der SDB; Ursache war Kicker-CSV-Lag plus fehlendes Deaktivieren von Abgängen.

**Kernaussagen:**
- Kaderquelle ist nur die Kicker-CSV `se-k00012026`. Daily-Fetch 09:00: 0 Zugänge, 0 Vereinswechsel. Nachmittags-CSV hatte Nkunku+Seol bereits; Seol fehlte lokal.
- Nkunku war seit dem Morgen als Leipzig in der API (`pl-k00091524`), Preis aber 0,5 Mio wegen Scout-Lock auf CSV-Sentinel 999.
- Seed/Push upserten nur. Abgänge (Palhinha, Wasinski) und Mention-Ghosts (Goretzka ohne Kicker-ID) blieben `active=1`.
- Goretzka: User hat recht, seit Juni weg; nie an offizielle CSV-ID gematcht (`manual-623`).

**Entscheidungen:**
- CSV-Overlay nach jedem Ingest: Club/Preis aus Live-CSV, nicht mehr gelistete IDs `active=false`.

**Konkrete Artefakte / Befehle / Pfade:**
- `backend/app/main.py` – `_apply_official_squad_overlay`, `POST /api/admin/ingest/refresh-squad`
- `docs/kader-ingest.md`
- Version `v.0827.001`
- kicker-scout: `fetch_kicker_data.py` + `app/seed.py` + `push_to_kmi.py` → Overlay 563 refresh, 48 deaktiviert
- Live: Nkunku RBL 4,7 aktiv, Seol FCA 1,7 aktiv, Palhinha/Wasinski/Goretzka inactive
- `ssh dockerhost274-kmi` – `docker compose up -d --build backend frontend`

**Offen / Naechste Schritte:**
- Git: viele lokale/untracked Änderungen (Pitch/Bank/Scoring + heutiges Overlay) – Commit nur nach Freigabe
- Chelsea-Nkunku-Dublette `manual-592` bleibt inactive
