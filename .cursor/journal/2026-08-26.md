# Journal 2026-08-26 – KMI Orchestrator

## Session 22:41 – Transfer-Punkte Fallback auf Feld/Liste

**Thema:** Transfers ohne Kicker-Jahr zeigten 0 auf dem Trikot; UI zeigt jetzt xPunkte, sonst 25/26.

**Kernaussagen:**
- Pkt 25/26 = 0 bei Transfers (CSV) ist Daten-korrekt, nicht ein Anzeige-Bug der Rohdaten.
- Chip/Bank in Saison-Sicht fallen auf xPunkte zurück wenn 25/26 leer ist (Karetsas, Veerman, Lochoshvili).
- Prognose-Sicht zeigt xPunkte und fällt auf 25/26 zurück wenn das Modell 0 ist.
- Listen-Spalte Pkt 25/26 nutzt denselben Fallback (blau = xPunkte); xPunkte-Spalte fällt auf 25/26.

**Konkrete Artefakte / Befehle / Pfade:**
- `frontend/src/lib/scoring.ts` – `viewDisplayPoints`, `preferredDisplayPoints`
- `frontend/src/components/PitchView.tsx`, `BankPanel.tsx`, `playerColumns.tsx`
- `docs/kader-ingest.md` – Abschnitt Anzeige
- Version `v.0826.006`
- `ssh dockerhost274-kmi` – `docker compose up -d --build frontend`
- Live: `http://192.168.178.251:3050/` HTTP 200, Footer `v.0826.006`

**Offen / Naechste Schritte:**
- Git: viele lokale/untracked Änderungen (u. a. `backend/app/main.py`, Pitch/Bank, tmp-Skripte) – Commit nur nach Freigabe
- Browser-Klickpfad (Trikot Karetsas) nicht per MCP verifiziert, nur HTTP/Version
